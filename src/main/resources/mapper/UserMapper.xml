<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.app.mapper.UserMapper">

<select id ="findByEmail" resultType="com.example.app.domain.User" parameterType="String">
   SELECT *FROM user
   WHERE email =#{email}
 </select>
 
 <insert id ="insert" parameterType="com.example.app.domain.User">
   INSERT INTO user(name,email,password,created_at)
   VALUES (#{name},#{email},#{password},#{createdAt})
 </insert>
 
 <select id="findByEmailAndPassword" resultType="com.example.app.domain.User">
   SELECT *FROM user
   WHERE email = #{email} AND password= #{password}
</select>
</mapper>
